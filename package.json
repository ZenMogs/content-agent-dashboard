{
  "name": "content-agent-dashboard",
  "version": "1.0.0",
  "scripts": {
    "dev": "node scripts/fetchData.js",
    "build": "mkdir -p public && cp dashboard/index.html public/index.html && cp dashboard/data.json public/data.json 2>/dev/null || true"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "node-fetch": "^2.6.7"
  }
}
